# INC-037 下書きに増えた記録の分だけを足す

| 項目 | 内容 |
|---|---|
| Issue | #37 評価セットの下書きに、増えた記録の分だけを足せるようにする |
| PR | [#44](https://github.com/yadori-dev/yadori/pull/44) |
| 選んだ受入基準 | `AC-019` 追記で前回の問と確認が保たれ、増えた分だけが足される |

## 体験の変化

保守する開発者は、前に作った下書きと同じファイルを指して追記する。前回の範囲（どの時刻まで、どのディレクトリから読み、どのファイルを飛ばしたか）より後に増えた記録だけが読まれ、その発話だけが候補を引かれて判定へ渡り、新しい問が末尾に確認前として足される。前回の問とその確認（`confirmed`、消した問、書き足した禁止、変えた名前）は一字も変わらない。前回の範囲は下書きに残り、人が読め、次の追記はそこから読む。

記録が増えるたびに別のファイルへ作り直して確認を手で写す作業と、既に確かめた分の費用を二度払うことが無くなる。代わりに、人が消した問の発話は下書きに無いので追記で戻らない。また追記のたびに、合わせた並びの末尾の直近往復数ぶんを指す組は問にならず、次回以降も拾い直さないので、少しずつ追記するより多く貯めてから追記する方が問は多く取れる。取りこぼしが気になれば新しいファイルへ作り直す。

追記できるのは、前回の範囲を書く版の道具が作った下書きだけである。前回の範囲を持たない下書き（それより前の版で作ったもの）には足せず、一度作り直す。埋め込みの AIモデルの名前か動かす道具の名前、条件、判定に使う AIモデルが下書きの前回の範囲と違うとき、記録のディレクトリが前回と違うときも足さず、新しいファイルへ作り直すよう伝える。埋め込みを動かす道具の版だけが違うときは足し、版が変わったことを画面に注意として出す。依存を上げるたびに確認済みの問が無駄になるのを避けるためである。混ぜると何で引いた下書きか読めなくなる。手書きの評価セットにも足さない。

## 入口の例

```console
$ uv run python -m yadori evals draft --from <記録のディレクトリ> --out <手元>/real-recall.toml --append
候補を引いた 埋め込み: paraphrase-multilingual-MiniLM-L12-v2（fastembed-0.8.0） / 条件: 直近6往復・候補10件・下限0.3 / 判定: opus
前回の範囲: 2026-08-31 15:20 まで、<記録のディレクトリ>、飛ばしたファイル 0、28 セッション
前回の下書き: 覚えさせる発話 106 件、問 44 問
今回: 新しい記録 3 セッション、新しい発話 21 件（読めず飛ばしたファイル 0）、判定へ渡した発話 15 件、候補が無く渡さなかった発話 6 件
覚えさせる発話: +17 件
問: +4 問（すべて確認前）。前回の 44 問とその確認はそのまま
  <手元>/real-recall.toml の新しい問を読み、残す問は confirmed = true にしてください

$ uv run python -m yadori evals draft --from <記録のディレクトリ> --out <手元>/hand-written.toml --append
下書きを作れません: <手元>/hand-written.toml は前回の範囲を持たないので追記できません。新しい版の道具で作り直してください
```

数値は増分の中で決める。読み取れることは、前回の範囲、今回何が増え、何を判定へ渡し、何が変わらなかったかである。

## 手段の候補

- `IDEA-037-001`: 判定の会話を対話する道具の継続（`--resume`）で続け、新しい問いだけを渡す。試したところ会話は続く。ただし今の判定は「後の発話と数件の候補」の小さな問いを十ずつ渡す独立した呼び出しで、前の会話を渡し直していない。継続で減るのは前置き（[ADR-016](../010_decisions/ADR-016-応対は対話する道具を通して作り、その道具の文脈を持ち込まない.md) の実測で三百語ほど）だけで、逆に会話を続けると前の問いが文脈に積み上がり、呼ぶたびの量は増える側である。会話の識別子を下書きへ残す仕組みと、応対は一往復ごとに新しく呼ぶ判断との線引きも要る。採らない。既に確かめた分の費用を二度払わないことは、増えた記録だけを判定することで満たす
- `IDEA-037-002`: 下書きに前回の範囲（読めた一往復の最大の時刻、読んだディレクトリ、飛ばしたファイル、セッション数）を残し、その時刻より後、または前回飛ばしたファイルの記録だけを新しい発話とする。採る
- `IDEA-037-003`: 前回のやりとりと問は読み込んでそのまま書き戻し、新しいやりとりと問を末尾に足す。使った番号の最後も下書きに残し、消された問の番号を使い回さない。採る
- `IDEA-037-004`: 候補を引く記憶は、前回の下書きにある覚えさせる発話と、今回の新しい発話だけで組む。記録は毎回全部読み直すが、それは前回の発話の作業場所と時刻を知るためで、下書きの覚えさせる側に無い発話（前回の問、人が消した問）は記憶に入れない。判定へ渡すのは新しい発話だけにする。代償は四つ。記録の読み直しと埋め込みの作り直しは記録の総量に比例して伸びる（数百発話で数十秒の見込み。測っていない）。記録が消えたり移されたりすると前回と同じ候補の引き方にならない（ディレクトリが違えば断る）。途中で失敗すると新しい分の判定は捨てになる。追記のたびに末尾の直近を指す組は問にならず拾い直さない。採る
- `IDEA-037-005`: 追記は道具が作った下書きにだけ受け付け、埋め込みの AIモデルの名前と動かす道具の名前、条件、判定の AIモデル、記録のディレクトリが前回と同じときだけにする。埋め込みを動かす道具の版（インデックスの名前に含める）は比べず、違えば注意を出す。違うときは新しいファイルへ作り直す。判定の指示の書き方が変わったことは道具が見分けられないので、その場合は人が作り直す。採る

## 要件とシステムテスト

| 要件と根拠 | 必要な振る舞い |
|---|---|
| `SPEC-037-001`（`AC-019`）追記の対象と範囲 | `--append` で既にある下書きを指す。下書きは前回の範囲（読めた一往復の最大の時刻、読んだディレクトリ、読めず飛ばしたファイル、セッション数）と、何で候補を引き何で判定したか（埋め込みの AIモデルの名前、それを動かした道具の名前と版、条件、判定の AIモデル）と、やりとりと問それぞれで使った名前の最後の番号を人が読める形で持つ。それを持たないファイルには何も変えずに理由を返す。埋め込みの AIモデルの名前か動かす道具の名前か条件、判定の AIモデル、記録のディレクトリの集まり（順序は問わない）が今と違うときも何も変えずに理由を返し、新しいファイルへ作り直すよう伝える。埋め込みを動かす道具の版だけが違うときは断らず、画面に注意を出す。新しい発話は、前回の時刻より後に記録された一往復と、前回飛ばしたファイルの一往復のうち、中身のあるものに絞り、同じ文言は最初の一つにし、前回の下書きにある発話と同じ文言のものを除いたものである |
| `SPEC-037-002`（`AC-019`）前回の問と確認を保つ | 前回のやりとりと問（名前、発話、返事、期待、禁止、確認の印、語の重なりの度合い、並び）は一字も変えずに残す。人が名前を変えた問、消した問、順序を変えた問もそのまま。新しいやりとりと問は末尾に足し、名前は前回使った最後の番号の次から付ける。消された問の番号は使い回さない。新しいやりとりは記録の時刻の順に並べて末尾に足す。前回飛ばしたファイル由来で前回の時刻より古い発話も末尾側に置くが、候補は記録の時刻で自分より前の発話からだけ引く。問を外した後に残る並びの末尾から既定の直近往復数の範囲に期待がある組は、前回と今回を合わせた並びで当て直して外す。追記した下書きは、全問を確認済みにすれば今までどおり測れる（指す先が揃い、名前が重ならない） |
| `SPEC-037-003`（`AC-019`）判定へは新しい発話だけ | 候補は、前回の下書きにある覚えさせる発話と今回の新しい発話（同じ作業場所）だけから引く。前回の問の発話と人が消した問の発話は覚えさせる側に無いので候補にならない。判定の問いにするのは新しい発話だけで、前回の問の発話は渡さない。新しい発話が前回の覚えさせる発話を指す組は問になる。候補が無い新しい発話は判定へ渡さず、その数を画面に出す |
| `SPEC-037-004`（`AC-019`）何が起きたかを伝え、途中の失敗で変えない | 画面に、前回の範囲（時刻、ディレクトリ、飛ばしたファイルの数、セッション数）と前回の下書きから数えた覚えさせる発話の数と問の数、今回増えた記録の数と新しい発話の数、判定へ渡した発話の数と候補が無く渡さなかった発話の数、足した発話と問の数、前回の問と確認がそのままであることが出る。渡した数と渡さなかった数の和は新しい発話の数（同じ文言を除いた後）と一致する。新しい問が一つも無くても前回の範囲は更新して書き、問が増えなかったことを画面に出す（新しいファイルを作るときの「問がゼロなら書かない」「既にあるファイルには書かない」は追記には当てはめない）。判定、埋め込み、記録の読み取りが途中で失敗したときは、下書きは一字も変わらない |

### 個々のシステムテスト

記録は架空の会話で作る。候補は決まった値を返す文字の埋め込みと下書き用の条件で引き、判定は答えを固定したものに差し替える。

| テスト | 対応 | 観点 | 入力 | 期待する結果 |
|---|---|---|---|---|
| `ST-037-001` | `SPEC-037-002` | 前回の内容が保たれ、新しい問が末尾に足され、測れる | 一度作った下書きに、確認済みの問、消した問（最後の番号）、名前を変えた問、順序を変えたやりとり、禁止を書き足した問を人の手で混ぜる。次に記録を増やして（前回より後の時刻、新しいセッション）追記する。全問を確認済みにして測る | 前回のやりとりと問は一字も変わらない。新しいやりとりと問が末尾に足され、名前は消した問の番号を使い回さず次から続く。新しい問は確認前である。前回の発話と同じ文言の新しい発話は足されない。全問を確認済みにすると測れる。合わせた並びの末尾の直近に期待がある組は問にならない |
| `ST-037-002` | `SPEC-037-003` | 判定へ渡るのは新しい発話だけ | 渡されたものを記録する判定の口を差し込んで追記する。新しい発話に、前回の覚えさせる発話を指すもの、前回の問の発話や人が消した問の発話と同じ話題のもの、候補が一つも上がらないもの、前回の最大の時刻の発話と同じ文言のもの、相槌だけのものを混ぜる | 判定の問いの発話はすべて新しい発話で、前回の問の発話や同じ文言の発話、相槌だけの発話は含まれず、相槌は新しい発話の数にも入らない。前回の問の発話と消した問の発話は候補にも上がらない。前回の覚えさせる発話を指す組が問になる。画面の「判定へ渡した発話」と「候補が無く渡さなかった発話」の和が新しい発話の数と一致する |
| `ST-037-003` | `SPEC-037-001` | 対象でないものには足さない（表駆動） | 手書きの評価セット、無いファイル、前回の範囲を持たない下書き、埋め込みの AIモデルの名前が違う下書き、条件（下限）だけが違う下書き、判定の AIモデルが違う下書き、記録のディレクトリを一つ足した追記、埋め込みを動かす道具の名前だけが違う下書き、にそれぞれ追記する。次に、埋め込みを動かす道具の版だけが違う下書きに追記する | 前の八つはいずれも何も変わらず、理由が返る。最後は足され、版が変わったことが画面に出る。前回の範囲を持たないときは作り直しを、埋め込み・条件・AIモデル・ディレクトリが違うときは新しいファイルへ作り直すことを、理由が伝える |
| `ST-037-004` | `SPEC-037-004` | 途中の失敗で下書きが変わらず、増えていなければ範囲だけ進む（表駆動） | 判定が失敗する、埋め込みが使えない、記録のディレクトリが無い、の三通りで追記する。次に、記録を増やさずに追記する | 前の三通りは下書きが一字も変わらず理由が返る。最後は問が増えず、前回の範囲だけが更新され、画面に問が増えなかったことが出る |
| `ST-037-005` | `SPEC-037-001` `SPEC-037-004` | 範囲の境界どおりに読み、画面で何が起きたかが読める | 前回の範囲の中、前回の時刻と同じ時刻、後、の記録と、前回飛ばして今回は読めるファイル、今回途中で切れているファイルを置いて追記する | 範囲の中と同じ時刻の記録は新しい発話にならず、後の記録と前回飛ばしたファイルの記録は新しい発話になる。新しいやりとりは時刻の順に末尾へ並び、前回飛ばしたファイル由来の古い発話が指すのは自分より時刻が前の発話だけである。今回切れたファイルは飛ばされ、どのファイルを飛ばしたかが前回の範囲に残り、次の追記で読み直される。画面に前回の範囲、今回増えた記録の数、判定へ渡した発話の数、足した発話と問の数、前回の問と確認がそのままであることが出る |

## 設計と結合テスト

### 設計判断

| 設計判断 | 実現する要件 | 構造と責務 |
|---|---|---|
| `AD-037-001` 前回の範囲は下書きの中の `[covered]` 表として道具が持ち、値は domain の `Covered` で、初回から書く | `SPEC-037-001` | 下書きの冒頭、`within` の次に `[covered]` の表を置き、読めた一往復の最大の時刻（`until`、記録の時刻を時差付きでそのまま）、読んだディレクトリ（`places`、絶対パスに解いた並び）、読めず飛ばしたファイル（`skipped`、絶対パスの並び。数ではなく、どのファイルかを残す。次の追記で読み直すために要る）、読んだセッションの数（`sessions`）、やりとりと問で使った名前の最後の番号（`last_exchange`、`last_case`。どこまで使ったかという範囲の一部として、ここに持つ）と、候補の引き方（`drawn_with`）を書く。引き方は一行のインラインの表（`drawn_with = { ai_model = "…", tool = "fastembed", tool_version = "0.8.0", recent = 6, limit = 10, floor = 0.3, judge = "…" }`）で書き、下位の表 `[covered.drawn_with]` にはしない。AIモデルを使わない埋め込みでは `ai_model` の鍵を省き、`tool` と `tool_version` は必ず書く。追記が `[covered]` を置き換える切り方（次の見出しの手前まで）が壊れるからである。人が読める形であり、表の上に「道具が書く前回の範囲。追記はここから読む。人は変えない。この表と次の見出しの間に書いた行は追記で消える」と断る。domain の evaluation には範囲の値 `Covered` と、引き方の値 `DrawnWith`（埋め込みの出自 `Provenance`、思い出し方 `HowToRecall` をそのまま、判定の AIモデルの名前）を置き、`Covered` が `DrawnWith` を持つ。思い出し方をばらして持たないのは、不正な条件を表せる形を新しく作らないためである。追記を断るか決めるのは `DrawnWith.differs_from(other)` で、埋め込みの AIモデルの名前と道具の名前、思い出し方、判定の AIモデルを比べ、違うところを文で返す（同じなら無し）。道具の版だけは比べず、違えば `DrawnWith.tool_version_changed(other)` が注意の文を返し、画面に出す。版で断ると依存を上げるたびに確認済みの問が無駄になるからで、同じ AIモデルを同じ道具で動かすなら版が違っても候補の引き方の差は小さいと見る（測っていない）。道具の名前は比べるので、AIモデルを使わない埋め込みどうしの取り違えも断れる。ディレクトリの集まりは `Covered` が順序を問わず比べる。新しく作るときも同じ表を書く。今まで冒頭の注釈に書いていた「候補を引いた 埋め込みと条件」は表に移して注釈から外す（同じことを二箇所に書くと、片方だけ直る）。画面には今までどおり出す |
| `AD-037-002` 新しい発話は手順が前回の範囲と前回の下書きから選び、数は選んだ場で数え、書くのは最後の一度 | `SPEC-037-001` `SPEC-037-004` | 下書きを作る手順（`Drafting`）に追記の入口（`append(places, out)`）を足す。最初に下書きを読み、その `DrawnWith` が今の埋め込み・思い出し方・判定の AIモデルと違うか、ディレクトリの集まりが違えば、記録を読む前に `CannotDraft` で断り、新しいファイルへ作り直すよう伝える。判定の指示文の変わりは見分けない（`IDEA-037-005`）。記録は今までどおり全部読み直す（時刻の順、飛ばしたファイルはパスで集める）。新しい一往復は「時刻が前回の `until` より後」または「ファイルが前回の `skipped` にある」もの。そのうち中身のあるものに絞り、同じ文言を最初の一つにし、さらに前回の下書きにある発話（覚えさせる発話と問の発話の両方）と同じ文言のものを除く。除いた後の数が「新しい発話の数」で、判定へ渡した数と候補が無く渡さなかった数の和はこれに一致する。`until` は前回読んだ全記録の最大の時刻なので、同じ時刻の記録は前回すべて読んでいる。境界を「同じか後」にすると、最後の記録から出た問を人が消したとき、その発話が次の追記で戻る。今回増えた記録のセッション数は、新しい一往復（中身で絞る前）が属するセッションの数。次に書く前回の範囲は、今回読み直した全記録から求める（`until` は全記録の最大の時刻、`sessions` は全記録のセッション数、`skipped` は今回飛ばしたファイル、`places` は今回指したディレクトリ）。下書きの追記の口を呼ぶのは手順の最後の一度だけで、読み取り、埋め込み、判定のどこで失敗しても下書きは触られない |
| `AD-037-003` 候補を引く記憶は前回の覚えさせる発話と新しい発話だけで組み、前回の発話の居場所は記録の読み直しで知る | `SPEC-037-003` | 前回の下書きの覚えさせる発話は作業場所と時刻を持たないので、今回読み直した記録から同じ文言の一往復（最初の一つ）を探して作業場所と時刻を得る。返事は下書きにあるものを使う（測るときも下書きの返事で覚えさせるため、揃える）。記録に見つからない前回の発話（記録が消えた、移された）は記憶に入れず、候補にならない。これは `IDEA-037-004` で引き受けた代償であり、断らない。作業場所ごとに、見つかった前回の発話と新しい発話を時刻の順に並べ、使い捨ての記憶へ順に入れる。新しい発話はまず思い出して候補を得てから覚えさせ、前回の発話は覚えさせるだけで問いにしない。並びが時刻の順なので、前回飛ばしたファイル由来の古い発話も、自分より時刻が前の発話からしか候補を引かない。前回の問の発話、人が消した問の発話は下書きの覚えさせる側に無いので記憶に入らない。問いにするのは候補のある新しい発話だけで、候補の無い新しい発話は数えて手順の結果に返す。思い出し方、判定への渡し方（十ずつ、発話と候補の文章だけ）は新しく作るときと同じ部品を使う |
| `AD-037-004` 合わせた並びで解き、直近で問にならない発話は覚えさせる側に残し、名前は前回の最後の番号から続け、合わせた評価セットで確かめてから書く | `SPEC-037-002` | 解く対象の並びは、前回のやりとり（下書きの順のまま）の後に新しい発話を時刻の順で置いたもの。並びの要素は一つの形（発話、返事、既にある名前があればその名前）にし、前回のやりとりは名前付き、新しい発話は名前無しで並べる。組を解く責務は手順の中の一つの部品で、この並び、判定の組、名前を始める番号（`last_exchange`、`last_case`）を受け取り、やりとりと問を返す。新しく作るときは、前回が空で番号が 0 から始まる追記として同じ部品を通す。解く規則を `run` と `append` で二本持たない（片方だけ直る事故を避ける）。組の解き方（期待をまとめる、問と期待の両方になる発話は期待に残す、末尾の直近に期待がある組を外す、期待の無い問を落とす）はこの合わせた並びに当てる。前回のやりとりは後の発話になり得ないので、まとめと期待残しの影響は受けない。末尾の直近は合わせた並びの末尾から数えるため、新しい発話が少ないときは前回の末尾のやりとりが直近に入り、そこを指す組は外れる。測るときも同じ並びで覚えさせて思い出すので、これで測れる問だけが残る。**期待が一つも残らず問にならなかった発話は、覚えさせる発話として並びに戻す。** 戻さないと、追記ではその発話の時刻は次回の `until` より前で下書きにも無く、追記を重ねるたびに最大で直近往復数ぶんの発話が黙って消えて以後の候補にもならない。直近を数える並びは戻す前のもの（問を外した後の並び）で、戻した後に数え直さない。戻した発話は自分の期待より後ろにあるので、末尾から数えた位置は戻した発話より前のものだけが一つ遠くなり、既に残った問の期待が直近に入ることはない（測れる問が減ることはなく、戻さずに済んだはずの問がわずかに減り得るだけ）。この規則は新しく作るときにも同じに当たる（[INC-030](INC-030.md) の `SPEC-030-002`・`ST-030-002`・`AD-030-003`・`IT-030-003` も同じ規則で書かれている）。新しいやりとりの名前は `last_exchange` の次から、新しい問の名前は `last_case` の次から付け、書いた後の値を前回の範囲へ戻す。番号は下書きの中の名前から求めない（人が名前を変えても、消しても、使い回さない）。前回のやりとりと問に新しい分を足した評価セットを組み、`verify_pointing()`（指す先が揃い、期待と禁止が重ならず、名前が重ならない）を通してから書く。人が前回の分を壊していれば（消したやりとりを問がまだ指す、名前を重ねた）ここで `CannotDraft` になり、何も書かれない。新しい問がゼロでも断らず、前回の範囲だけを進めて書く |
| `AD-037-005` 下書きの口は書く・読む・追記するの三つを同じ評価セットの形で受け、追記は文字の並びのまま末尾へ足し、前回の分は一字も変えず、一時ファイルを差し替えて書く | `SPEC-037-002` `SPEC-037-004` | domain の下書きの口を、書くだけの `DraftWriter` から、下書きの置き場 `Drafts` に変える。口は三つ。書く（`write(path, recall_eval, covered)`）、読む（`read(path)`、前回の評価セットと `Covered` を返す）、追記する（`append(path, added, covered)`、`added` は足す分 `Added`。末尾に足すやりとりと問の並びを持つ専用の値）。足す分を評価セットの形にしないのは、新しい問の期待が前回のやりとりを指すため、足す分だけでは指す先が揃わず、評価セットとしては必ず不正になるからである。合わせた評価セットの確かめは手順が書く前に済ませている（`AD-037-004`）。実装は adapter の `DraftFile` が引き続き持つ。読むときはやりとりと問の読み取りに評価セットの読み手（`EvalFile`）を使い、その失敗（`CannotMeasure`）は下書きの失敗（`CannotDraft`）に言い換える。人が前回の問をすべて消した下書きは普通にあり得るので（読んで、違えば消す、が想定した操作）、`EvalFile` は問が一つも無くても読めるようにし、問が無いことを断るのは測る手順（`Measuring`）の確認前を断る段と同じ独立した一段へ移す。読み手が判断しないという `AD-030-004` の決めにも揃う。`EvalFile` は語の重なりの度合い（`overlap`）を読まないままにする。追記は文字の並びを足すだけで書き戻す道が無く、測る側も使わないからである。新しい問に書く語の重なりの度合いの見出しは、人が変えたやりとりの名前が裸の見出しで書けない文字（日本語など）を持ち得るので、引用して書く。道具が自分の書いたもので下書きを壊さないためである。`[covered]` の表が無ければ「前回の範囲を持たないので追記できない。新しい版の道具で作り直す」と断る。表の欄が欠けていても同じ系列の文面で断る（`ai_model` は省けるので欠けに数えない）。追記は評価セットの値を書き直さない。前回の分を値に読んで書き戻すと、人が書いた注釈、並び、書き方が消えるからである。代わりに、ファイルの文字の並びを読み、`[covered]` の行からその次の表の見出し（`[` で始まる行）の手前まで（見出しが無ければ末尾まで）を新しい表に置き換え、それ以外は一字も変えず、新しいやりとりと問の塊を末尾に足す。TOML は同じ名前の配列の表が離れて続くことを許すので、`[[case]]` の後に `[[exchange]]` が来てもやりとりの順は保たれる。追記のときは同じディレクトリに一時ファイルを書いてから差し替える（`os.replace`）ので、書く途中で落ちても元の下書きは残る。新しく作るときは既にあるファイルを断るので差し替えは要らない。出力先の境界（リポジトリの配下、ディレクトリ）は新しく作るときと同じく断り、「既にあるファイルには書かない」は追記では逆に「無ければ断る」になる |
| `AD-037-006` 入口は `evals draft` の中でだけ、値を取らない引数として `--append` を読み、追記の結果を伝えるのは infrastructure | `SPEC-037-004` | 入口（`Entry`）は `evals draft` に続く引数の中でだけ、値を取らない引数（今は `--append` だけ）を先に拾ってから、残りを名前と値の対として読む。`evals draft` の引数の中ならどこに置いてもよい。二度書けば `--out` を二度書いたときと同じく使い方を標準エラーへ書いて 1 を返す。`measure` など他の下位命令に付いていれば、知らない名前として今までどおり使い方を出して 1 を返す。追記の結果は手順が値（`Appended`）で返す。前回の範囲、前回の下書きの覚えさせる発話の数と問の数、今回増えた記録のセッション数、新しい発話の数、読めず飛ばしたファイルの数、判定へ渡した数、候補が無く渡さなかった数、足したやりとりと問の数、埋め込みを動かす道具の版が変わったときの注意。画面へ出すのは `Drafter` で、候補を引いた行（埋め込みの AIモデルの名前、括弧で道具の名前と版、条件、判定の AIモデル。AIモデルを使わない埋め込みは名前の代わりに「AIモデル無し」。新しく作るときも同じ一行。断るときの文面も前回と今回の両方の名前を出す）、前回の範囲の行（時刻、ディレクトリ、飛ばしたファイルの数、セッション数）、前回の下書きの行、今回の行、足した数の行、問が増えたなら確認の促し、増えなかったならその旨、前回の問と確認がそのままである旨を書く。下書きの失敗と埋め込みが使えない失敗は今までどおり理由を標準エラーへ書いて 1 を返す。使い方に `--append` と、前回の範囲を持つ下書きにだけ足せることを書く |
| `AD-037-007` 何で引いたかは口が自分で答え、下書きの口と手順の返す値はそれを受ける形に変える | `SPEC-037-001` | 埋め込みの口（`Embeddings`）に、何で作ったかを一つの値で答える欄 `provenance` を足す。値は domain（memory）の `Provenance` で、AIモデルの名前（`ai_model`。AIを使わない埋め込みは無し）、動かした道具の名前（`tool`）、その版（`tool_version`）の三つを持つ。インデックスの名前を組む規則は `Provenance` 自身が持つ（`index_name`。AIモデルの名前があれば `AIモデルの名前/道具-版`、無ければ `道具-版`）。各実装は出自だけを答え、名前 `name` はそこから組む派生物にする。規則が実装ごとにあると片方だけ変わってインデックスの名前が黙ってずれるからである。意味を見る埋め込みの AIモデルの名前は今の名前の前半（提供元の接頭辞を落とした短い名前）と同じにし、道具は `fastembed` と版なので、インデックスの名前は今と変わらない。文字の並びだけ見る埋め込み（`CharacterPairs`）は AIモデル無し、道具 `character-pairs`、版 `v1` で、名前は今と同じ `character-pairs-v1`。手順が名前を割って版を取り出すのではなく、埋め込み自身に答えさせる。判定の口（`Judge`）に名前（`name`）を足す。対話する道具で判定する実装（`ClaudeCodeJudge`）は、呼び方の部品（`ClaudeCodeCall`）が実際に使う AIモデルの名前を読み取り専用の欄（`model`）として出したものを、そのまま返す。判定の実装が受ける呼び方の口（`_Call`）にもこの欄を含める。名前を別に渡す形にすると、下書きに残る名前と実際に判定した AIモデルがずれ得るからである。`Covered` を組むには埋め込みの出自と判定の名前が要り、下書きの口の形は `AD-037-005` に書く。手順が返す値 `Draft` の飛ばしたファイルは数からパスの並びへ変え、画面は数を出す。同じ形の変更が [INC-030](INC-030.md) の `ST-030-004`・`AD-030-001`・`AD-030-006` にも当たる |

### 個々の結合テスト

| テスト | 対応 | 観点 | 入力 | 期待する結果 |
|---|---|---|---|---|
| `IT-037-001` | `AD-037-001` | 前回の範囲が初回から下書きに残り、読み戻せ、違いを自分で答える | 決まった判定で新しい下書きを書き、下書きの口で読み戻す。次に、引き方を埋め込みの AIモデルの名前だけ、道具の名前だけ、下限だけ、判定の AIモデルだけ、道具の版だけ変えたものと比べ、ディレクトリを一つ足したもの、同じディレクトリを逆順に並べたもの、それぞれと比べる。次に、AIモデルを使わない埋め込みの出自で書いた下書きを読み戻す。次に、`[covered]` の無い評価セットと、欄の欠けた表を持つファイルを読む | 読み戻した `Covered` は書いたものと一致し、時刻は時差付きで元の値のまま、飛ばしたファイルはパスのまま、思い出し方は `HowToRecall` として、埋め込みの出自は `Provenance` として戻る。冒頭の注釈に埋め込みの名前は無く、表にある。引き方の前四つは違いが文で返り、道具の版だけの違いは断る違いにならず注意の文が返り、ディレクトリを足したものは違うと答え、逆順は同じと答える。AIモデル無しの出自は `ai_model` の鍵が無い表として書かれ、読み戻すと AIモデル無しの出自として戻る。後二つは前回の範囲を持たない理由の `CannotDraft` になり、作り直しを伝える |
| `IT-037-002` | `AD-037-002` | 新しい発話の境界と数え方、書くのは最後の一度 | 前回の範囲を持つ下書きと、その `until` より前・同じ・後の一往復、前回飛ばしたファイルの一往復、前回の覚えさせる発話と同じ文言の後の一往復、前回の問の発話と同じ文言の後の一往復、相槌だけの後の一往復、同じ文言が二つある後の一往復、を混ぜた記録を作り、渡されたものを記録する判定で追記する。次に、引き方の埋め込みの名前が違う下書きへ、記録の読み取りを数える読み手で追記する。次に、判定が `CannotDraft` を投げる追記と、埋め込みが `EmbeddingsUnavailable` を投げる追記 | 新しい発話は「後・飛ばしたファイル由来」から相槌と同じ文言を除いたものだけで、`until` より前と同じ時刻のものは入らない。判定へ渡した数と渡さなかった数の和が新しい発話の数に一致する。今回増えたセッション数は新しい一往復のセッションの数。書き戻された前回の範囲は、`until` が全記録の最大の時刻、`sessions` が全記録のセッション数、`skipped` が今回飛ばしたファイル。埋め込みの名前が違うときは読み手が一度も呼ばれずに `CannotDraft` になり、新しいファイルへ作り直すことを伝える。判定と埋め込みの失敗はどちらも呼び出し側へ届き、下書きの文字の並びは一字も変わらない |
| `IT-037-003` | `AD-037-003` | 記憶に入るものと問いになるものが分かれ、居場所は記録から引き、返事は下書きから取る | 二つの作業場所の記録と、片方の作業場所の覚えさせる発話と問を持つ前回の下書きを作る。前回の覚えさせる発話の一つは記録から消し、別の一つは下書きの返事を記録と違えておく。前回飛ばしたファイルに、前回の時刻より古い一往復を置く。渡されたものを記録する判定と、覚えさせた返事を見られる記憶で追記する | 問いの発話はすべて新しい発話で、前回のやりとりの発話は問いにならない。候補は同じ作業場所の、時刻が前のものだけで、前回の覚えさせる発話が候補に現れ、前回の問の発話は現れない。記録から消した前回の発話は候補に現れず、失敗にもならない。返事を違えた前回の発話は下書きの返事で覚えさせられている。前回飛ばしたファイル由来の古い新しい発話は、自分より後の時刻の発話を候補に持たない |
| `IT-037-004` | `AD-037-004` | 合わせた並びで解け、直近で外れた発話が残り、番号が続き、壊れた前回の分では書かない | 前回のやりとり `e001`〜`e020`（直近往復数は既定の 6）、名前を `c-renamed` に変えた問、消した問（`last_case` は 5 まで進み、残る問は 3 つ）を持つ下書きに、新しい発話 3 件を足す記録を用意し、判定が「新1 → `e005`」「新2 → `e020`」を返し新3 には何も返さないようにして追記する。次に、消したやりとりを問がまだ指す前回の下書きに追記する | 新1 → `e005` の組は問 `c006` になり、期待は `e005` を指す。新2 → `e020` は `e020` が問を外した後（戻す前）の並び（`e001`〜`e020`、新3）の末尾 6 件に入るので問にならず、新2 は覚えさせる発話 `e021` として残る。新3 は `e022` になる。名前は消した問の番号 `c004`・`c005` も `c-renamed` も使い回さない。書かれた下書きは `EvalFile` で読め、全問を確認済みにすると測れる。壊れた前回の分は `CannotDraft` になり、ファイルは一字も変わらない |
| `IT-037-005` | `AD-037-005` | 前回の分が一字も変わらず、途中で落ちても元が残り、問ゼロの前回を読め、境界を守る | 人の手で注釈、空行、やりとりの名前を日本語に変えたもの、順序の入れ替え、`confirmed = true` を混ぜた前回の下書きへ、新しいやりとりと問を追記する。次に、前回の問をすべて消した下書きへ追記する。次に、書く途中で失敗する差し替えで追記する。次に、無いファイル、ディレクトリ、`.git` を持つディレクトリの配下のファイル、`[covered]` の無いファイルへ追記する | `[covered]` の塊の外の文字の並びは、前回のものが末尾までそのまま残り、その後に新しい塊が続く。`[covered]` は新しい値になる。日本語の名前を期待に持つ新しい問の語の重なりの度合いも書け、読み戻すと前回のやりとりと問の名前・発話・返事・期待・禁止・確認の印がすべて一致する。問をすべて消した下書きも読めて追記でき、測る手順が問ゼロを理由に断る。途中で失敗したときは元の文字の並びのまま。後四つは `CannotDraft` で、文面が理由を言い分ける |
| `IT-037-006` | `AD-037-006` | 値を取らない引数の読み方と、追記の結果の伝え方 | 決まった判定で入口から追記する。`--append` を先頭、途中、末尾に置く。次に二度置く。次に `measure --append` を呼ぶ。次に、新しい問が出ない記録で追記する。次に、`--append` を付けずに既にあるファイルを指す。次に、`--append` を付けて無いファイルを指す | 位置に関わらず同じ結果で、標準出力に候補を引いた行（埋め込みの AIモデルの名前と道具の名前と版、条件、判定の AIモデル）、前回の範囲、前回の下書き、今回、足した数、確認の促し、前回の問と確認がそのままである旨が出て 0 で終わる。二度置いたときと `measure --append` は使い方が標準エラーに出て 1 で終わる。問が出ないときは問が増えなかった旨が出て 0 で終わり、前回の範囲だけ進む。`--append` が無いときは既にあるファイルの理由、無いファイルのときは無い理由が標準エラーに出て 1 で終わる。使い方に `--append` がある |
| `IT-037-007` | `AD-037-007` | 口が答える値の形（出自、判定の名前、飛ばしたファイル） | 意味を見る埋め込みと文字の並びだけ見る埋め込みの出自と名前を読む。次に、対話する道具で判定する実装を AIモデルの名前を渡して作り、名前を読む。次に、読めないファイルを一つ混ぜた記録から新しい下書きを作る | 意味を見る埋め込みの出自は AIモデルの名前、道具の名前、版を持ち、名前は出自が組んだとおり（`AIモデルの名前/道具-版`）で今までと同じ。文字の並びだけ見る埋め込みの出自は AIモデル無しで、名前は出自が組んだとおり `character-pairs-v1`。判定の名前は渡した AIモデルの名前を含む。下書きの結果が持つ飛ばしたファイルは読めないファイルのパスの並びである |
