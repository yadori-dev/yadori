# セキュリティ

## 扱う情報

yadori は、利用者との会話をそのまま保存します。保存する内容には、利用者の私生活、仕事、人間関係、判断が含まれます。この前提でリポジトリと運用を扱います。

- 記憶の保存先はリポジトリの外に置く。`.gitignore` だけに頼らない
- 会話の実例を文書、Issue、PR、コミットメッセージへ写さない。必要な場合は架空の例に置き換える
- 外部サービス（Discord、模型の提供元）へ会話が渡ることを、利用者に分かる形で書く
- 認証情報を環境変数から読み、リポジトリとログへ出さない

## 脆弱性の報告

GitHub の Security Advisory から非公開で報告してください。Issue へは書かないでください。
