# 変更履歴

書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、版の付け方は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

1.0.0未満は初期開発版である。マイナー版の更新で、HTTP API、保存形式、出ていく先との約束が互換性なく変わることがある。互換性のない変更では、消えた入口、置き換える使い方、以前の保存先を読めるか、読めない場合にどう始めるかを書く。

**利用者の記憶の原文を失う移行はしない。** 新しい版が以前の保存先を読めない場合、そのファイルを変更・削除せず起動を断る。

## [Unreleased]

## [0.1.0] - 2026-09-14

起動方法と設定の互換性変更を含むマイナー版。記憶の原文は維持する。

### Added

- Linuxのagy 1.2.1 / 1.2.2を宿りとして起動し、同じ記憶と気持ちで会話する入口
- 夢で短い返答の対象を説明し、原文と根拠を合わせて思い出す処理
- Codex・Claude・agyが根拠不足のとき、候補検索と原文取得を自発的に行うMCP接続
- 通常のClaude・Codex・agyの完成した会話を、外部の出典付きで取り込む入口。確認、再実行時の重複防止、人物ごとの保存に対応
- 起動前の取り込みと自身のdream。人物ごとの自動・毎回選択・実行しない設定と、今回だけの `--prepare auto/ask/skip`

- `yadori setting` で人物と共通の道具順を対話で設定・編集する入口
- 未設定の対話起動から初回設定へ進み、確認保存後に元の起動を続ける流れ
- 人物の識別子を保持した編集と、人物ごとの記憶を引き継ぐ切り替え
- Codex・Claude Code・agy による内部の応対・要約・判定

### Changed

- MITライセンスで公開し、外部からのコード・ドキュメントの貢献を受け付ける
- READMEを導入と主要な使い方中心に整理し、詳細を利用ガイドへ分ける

- 引数なしの `yadori` は共通の優先順（既定 Codex → Claude Code → agy）で道具を起動する。従来の端末チャットは `yadori chat` へ移る
- 設定保存先は `settings.toml`。旧 `dweller.toml` と `identity.md` は編集保存時に取り込み、原本を残す。保存後の編集先は新ファイル。記憶の原文・状態・名乗りの版は移動も削除もしない
- 道具順の変更は次の処理開始から反映する。Discord では再起動する。評価下書きの追記で判定モデルが変わる場合は従来どおり別ファイルで作り直す

### Fixed

- 起動案内を `yadori` に統一し、`--help` を正常終了にする
- Claudeの初回案内済み設定を引き継ぎ、起動するたび案内へ戻らないようにする

### 更新時の操作

- 従来の端末チャットを続ける場合は `yadori chat` を使う。引数なしでは道具を選んで起動する
- `yadori setting` で既存の人物と設定を確認する。旧ファイルは編集保存時に新形式へ取り込み、元のファイルと記憶は残す
- 起動前の準備は初回に取り込み元と動作を選んで保存する。今回は準備せず話す場合は `--prepare skip`
- 取り込んだ外部会話は自身の発言と区別し、取り込みだけで名乗り・気持ち・思い出した回数を変えない

## [0.0.1] - 2026-09-11

最初期リリース。仕様や保存形式は今後変わることがある。

### Added

- 端末と Discord からの会話、記憶と状態の共有
- Claude Code と Codex を宿りとして起動する入口
- 会話の保存と思い出す処理、気持ちと性格の状態表示
- 記憶を読み直して整理する夢の手動実行
- 思い出す質の測定と、評価セットの下書き作成
- Ubuntu 24.04 / 26.04 と Debian 13 の amd64 向け apt 配布
- Git タグから製品版を求め、Release 発行から配布物と署名付き apt 配布元を自動で更新する仕組み
- 進め方、要求分析、後戻りが高い判断、用語集、層の構造

[Unreleased]: https://github.com/yadori-dev/yadori/compare/v0.1.0...develop
[0.1.0]: https://github.com/yadori-dev/yadori/compare/v0.0.1...v0.1.0
[0.0.1]: https://github.com/yadori-dev/yadori/releases/tag/v0.0.1
