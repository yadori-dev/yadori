# yadori — エージェント向け運用規則

進め方の正典は [docs/000_process.md](docs/000_process.md) である。期待する体験はIssue、増分のV字はdocs/210_incrementsのINC、実装して確認した結果はPRに書く。

## 作業を始めるとき

- 最新のINCで現在の増分と節目を確認する
- 対応するIssueで人物、困りごと、体験の変化、入口例を確認する
- 長く生きる要求はdocs/110_requirementsを見る
- 使う言葉はdocs/150_system/用語集.mdを見る。ここに無い概念へ勝手に名前を付けない
- 現在の使い方はREADME、現在の構造はdocs/150_systemを見る
- 後戻りが高い判断は有効なADRを見る
- Git運用、環境構築、コードの書き方はCONTRIBUTING.mdを見る

## この製品で特に守ること

- **記憶の原文を失う変更をしない。** 要約、圧縮、整理は原文を残したまま別の層として足す
- **利用者の会話をリポジトリへ写さない。** 文書、Issue、PR、テスト、コミットメッセージのすべてで架空の例を使う
- **状態を上書きせず、出来事として積む。** 気持ちと性格の現在値は出来事から求める
- **記憶と状態は、宿りへ結ぶ。** 持ち主へ直接結ばない。宿りが一つしかない間も同じ
- **思い出す質を、文章の受入基準で判定しない。** 評価セットで測る（[ADR-010](docs/010_decisions/ADR-010-思い出す質を評価セットで判定する.md)）

## 毎回守ること

顧客名、参画先名、本人以外の個人名、非公開リポジトリのURL、社内のホスト名、契約条件、絶対パス、端末名を書かない。Issue、PR、Discussion、コミットメッセージも同じである。

コミットメッセージにAI生成フッターを含めない。mainとdevelopへ直接pushせず、featureブランチからdevelopへPRを出す。

現在の作業や進み具合を、このファイルへ書かない。
